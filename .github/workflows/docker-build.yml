name: Build and Push Docker Images

on:
  push:
    branches: [ main, master ]
    tags: [ 'v*.*.*' ]
  pull_request:
    branches: [ main, master ]

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository }}

jobs:
  build-node:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-node
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Node image
        uses: docker/build-push-action@v5
        with:
          context: ./node
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64

  build-elixir:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-elixir
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Elixir image
        uses: docker/build-push-action@v5
        with:
          context: ./elixir
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64

  test-images:
    needs: [build-node, build-elixir]
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    permissions:
      packages: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull images
        run: |
          docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-node:${{ github.ref_name }}
          docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-elixir:${{ github.ref_name }}

      - name: Test Node image
        run: |
          docker run -d --name test-node \
            -e DB_PATH=/data/push_server.db \
            -e FALLBACK_PORT=3000 \
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-node:${{ github.ref_name }}
          
          # Wait for container to start and be healthy
          sleep 10
          
          # Check if container is still running
          if ! docker ps | grep -q test-node; then
            echo "Container failed to start. Logs:"
            docker logs test-node
            exit 1
          fi
          
          # Wait for healthcheck to pass (with timeout)
          timeout 60 sh -c 'until docker inspect --format="{{.State.Health.Status}}" test-node 2>/dev/null | grep -q healthy; do sleep 2; done' || {
            echo "Health check failed. Container logs:"
            docker logs test-node
            exit 1
          }
          
          # Test health endpoint
          docker exec test-node curl -f http://localhost:3000/health
          
          docker stop test-node
          docker rm test-node

      - name: Test Elixir image
        run: |
          docker run -d --name test-elixir \
            -e DB_PATH=/data/push_server.db \
            -e PORT=4000 \
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-elixir:${{ github.ref_name }}
          
          # Wait for service to start (elixir needs more time)
          sleep 15
          
          # Test health endpoint
          docker exec test-elixir curl -f http://localhost:4000/health || true
          
          docker stop test-elixir
          docker rm test-elixir
